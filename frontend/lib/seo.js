// Single source of truth for the canonical production domain, used by
// metadata, the sitemap, robots.txt, and JSON-LD across the app. Update
// this once DNS for jobbers.be is actually connected — everything else
// derives from it.
export const SITE_URL = 'https://jobbers.be';
export const SITE_NAME = 'Jobber';

// Vercel is configured with www.services34.fr as the primary domain — the
// bare domain 308-redirects to it. SITE_URL must match what actually
// resolves without a redirect, or every sitemap URL and canonical tag
// points Google at a redirect instead of the real page (this is what
// produced the "Page avec redirection" bucket in Search Console).
export const SITE_URL = 'https://www.services34.fr';
export const SITE_NAME = 'Services 34';

// Selecting either of these as required equipment triggers the "Travail en
// hauteur prévu ?" question (bricolage/jardinage) — kept as a name match
// rather than hardcoded ids since Equipment rows are seeded per-environment.
export const WORK_AT_HEIGHT_EQUIPMENT_NAMES = ['Échelle', 'Escabeau'];
